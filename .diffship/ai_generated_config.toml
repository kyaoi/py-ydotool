# diffship AI-generated local configuration (generated by `diffship init`)
#
# Repository snapshot:
# - repo: py-ydotool
# - current branch: main
# - preferred promote target: main
#
# See also: docs/CONFIG.md
# This file is merged as a project-local config layer before `.diffship/config.toml`.
# Keep AI-editable defaults here when you want assistants to adjust local behavior
# without rewriting the user-owned config stub.
#
# Customize this section: opt specific generated `.diffship/*` files into AI editability.
# Only the fixed allowlist below is honored; other `.diffship/*` paths stay forbidden.
[ops.editable_diffship]
path1 = ".diffship/.gitignore"
path2 = ".diffship/AI_GUIDE.md"
path3 = ".diffship/forbid.toml"
path4 = ".diffship/PROJECT_KIT.md"
path5 = ".diffship/PROJECT_RULES.md"
path6 = ".diffship/ai_generated_config.toml"
# path7 = ".diffship/config.toml" # opt in only if you explicitly want AI to rewrite the user-owned config
#
# Customize this section: add AI-owned defaults that may change over time.
# Example:
# [handoff]
# default_profile = "10x100"
#
# [verify]
# default_profile = "fast"
